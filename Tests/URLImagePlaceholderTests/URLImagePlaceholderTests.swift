import XCTest
@testable import URLImagePlaceholder

final class URLImagePlaceholderTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(URLImagePlaceholder().text, "Hello, World!")
    }
}
